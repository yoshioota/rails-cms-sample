h1 = @site.title

p 最近更新したページ

- @site.pages.updated_at_desc.each do |page|
  div= link_to page.title, home_site_page_path(@site, page)

= link_to 'new page', new_home_site_page_path(@site)
|&nbsp;|&nbsp;
= link_to 'Edit', edit_home_site_path(@site)
|&nbsp;|&nbsp;
= link_to 'Back', home_sites_path
